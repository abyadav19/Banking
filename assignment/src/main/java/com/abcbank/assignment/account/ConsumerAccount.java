package com.abcbank.assignment.account;

public class ConsumerAccount extends Account {

	public ConsumerAccount(AccountHolder accountHolder, Long accountNumber, int pin, double balance) {
		super(accountHolder, accountNumber, pin, balance);
	}
}
